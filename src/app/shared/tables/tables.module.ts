import { NgModule } from '@angular/core';
import { AppTableComponent } from './tables.component'; // Adjust the path

@NgModule({
  declarations: [AppTableComponent],
  exports: [AppTableComponent],
})
export class AppTableModule {}
